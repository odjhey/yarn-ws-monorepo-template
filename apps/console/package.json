{
  "name": "@repo/console",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint src",
    "start": "node dist/src/index.js"
  },
  "devDependencies": {
    "@repo/cross-concerns": "*",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@repo/vitest-config": "*",
    "@types/ramda": "^0",
    "@vitest/coverage-v8": "^2.0.5",
    "@vitest/ui": "^2.0.5",
    "eslint": "8",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "ramda": "^0.30.1"
  }
}
